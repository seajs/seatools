#!/usr/bin/env node

var pkg = require('../package.json');
var isPlugin = pkg.name.indexOf('seajs-') > -1;

switch(process.argv[2]) {
  case 'local':
    test_local();
    break;
  case 'http':
    test_http();
    break;
  case 'totoro':
    test_totoro();
    break;
  default:
    test_node();
    test_local();
    test_http();
}

function test_local () {
  
}

function test_http () {
  
}

function test_totoro () {
  
}